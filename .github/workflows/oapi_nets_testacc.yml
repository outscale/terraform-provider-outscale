name: OAPI - Nets Acceptance Tests

on:
    pull_request:
        branches:
            - master
        paths:
            - ".github/workflows/oapi_nets_testacc.yml"
            - ".github/workflows/reusable_acceptance_test.yml"
            - "internal/services/oapi/**"
            - "internal/client/**"
            - "internal/testacc/**"
            - "internal/fwhelpers/**"
            - "internal/fwmodifyplan/**"
            - "internal/fwvalidators/**"
            - "internal/utils/**"
            - "provider/**"
            - "go.sum"
            - "go.mod"
            - "GNUmakefile"
    workflow_dispatch:

permissions:
    contents: read

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    oapi-nets-tests:
        uses: ./.github/workflows/reusable_acceptance_test.yml
        with:
            test-command: testacc-oapi-net
            region: us-east-2
            service: oapi
            providers: outscale_oapi,outscale_oos
        secrets: inherit
